[project]
name = "moon-gpt"
version = "0.1.0"
description = "Resume question-answering assistant for Chandramouli Das"
requires-python = ">=3.14,<3.15"
dynamic = ["dependencies"]

[tool.setuptools.dynamic]
dependencies = { file = ["requirements.txt"] }
